const home = {
	  namespace:true,
	  state: {
	  	title:'home'
	  },
	  mutations: { },
	  actions: { },
	  getters: { 
	  	getTitle(state){
	  		return state.title;
	  	}
	  }
}

export default home;